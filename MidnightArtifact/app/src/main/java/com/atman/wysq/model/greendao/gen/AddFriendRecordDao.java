package com.atman.wysq.model.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.atman.wysq.model.bean.AddFriendRecord;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ADD_FRIEND_RECORD".
*/
public class AddFriendRecordDao extends AbstractDao<AddFriendRecord, Long> {

    public static final String TABLENAME = "ADD_FRIEND_RECORD";

    /**
     * Properties of entity AddFriendRecord.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property ToRequest = new Property(1, String.class, "toRequest", false, "TO_REQUEST");
        public final static Property FromRequest = new Property(2, String.class, "fromRequest", false, "FROM_REQUEST");
    }


    public AddFriendRecordDao(DaoConfig config) {
        super(config);
    }
    
    public AddFriendRecordDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ADD_FRIEND_RECORD\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"TO_REQUEST\" TEXT," + // 1: toRequest
                "\"FROM_REQUEST\" TEXT);"); // 2: fromRequest
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ADD_FRIEND_RECORD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AddFriendRecord entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String toRequest = entity.getToRequest();
        if (toRequest != null) {
            stmt.bindString(2, toRequest);
        }
 
        String fromRequest = entity.getFromRequest();
        if (fromRequest != null) {
            stmt.bindString(3, fromRequest);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AddFriendRecord entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String toRequest = entity.getToRequest();
        if (toRequest != null) {
            stmt.bindString(2, toRequest);
        }
 
        String fromRequest = entity.getFromRequest();
        if (fromRequest != null) {
            stmt.bindString(3, fromRequest);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public AddFriendRecord readEntity(Cursor cursor, int offset) {
        AddFriendRecord entity = new AddFriendRecord( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // toRequest
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // fromRequest
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AddFriendRecord entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setToRequest(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setFromRequest(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AddFriendRecord entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AddFriendRecord entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AddFriendRecord entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
